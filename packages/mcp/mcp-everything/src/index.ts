/**
 * @deepseek-ai/dsh-mcp-everything — a config-only MCP bundle. The package's
 * substance is `cordis.patch.yml` (declared by the `dsh.bundle.patch` manifest
 * field) plus the bundled stdio MCP server under `server/`; this module
 * carries no runtime API.
 * @module @deepseek-ai/dsh-mcp-everything
 */

export {}
