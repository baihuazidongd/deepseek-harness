/**
 * @deepseek-ai/dsh-mcp-playwright — a config-only MCP bundle. The package's
 * substance is `cordis.patch.yml` (declared by the `dsh.bundle.patch` manifest
 * field); this module carries no runtime API.
 * @module @deepseek-ai/dsh-mcp-playwright
 */

export {}
