/** Host loader entry for the archive-recovery browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior; recovery writes ride the existing workspace wire methods. */
export function apply(): void {}
